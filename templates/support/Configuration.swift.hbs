//  
//  {{>frag/generatedBy}}
//

import Foundation

public typealias ConfigurationFinalizeReleaseBlock = ((_ request: inout URLRequest) -> Void)

public struct Configuration {

    /** The handler for all security requests. */
    public var securityClient: SecurityClient?

    /**
     * Override the default base path.
     */
    public var basePath: String?

    public var cachePolicy: URLRequest.CachePolicy?

    public var timeoutInterval: TimeInterval

    public var responseQueue: DispatchQueue?

    public var finalizeReleaseBlock: ConfigurationFinalizeReleaseBlock?

    public init(
        basePath: String? = nil,
        cachePolicy: URLRequest.CachePolicy? = nil,
        timeoutInterval: TimeInterval = 30,
        responseQueue: DispatchQueue? = nil,
        securityClient: SecurityClient? = nil,
        finalizeReleaseBlock: ConfigurationFinalizeReleaseBlock? = nil
    ) {
        self.basePath = basePath
        self.cachePolicy = cachePolicy
        self.timeoutInterval = timeoutInterval
        self.responseQueue = responseQueue
        self.securityClient = securityClient
        self.finalizeReleaseBlock = finalizeReleaseBlock
    }
}
