//  
//  {{>frag/generatedBy}}
//

import Foundation

extension Foundation.CharacterSet {

    /// The character set for percent encoding x-www-form-urlencoded data.
    static let formUrlEncoded: Foundation.CharacterSet = {
        var set = Foundation.CharacterSet.urlQueryAllowed
        set.remove("+")
        return set
    }()
}