{{!-- 
@param clientId the client ID for the OAuth flow
@param clientSecret the client secret for the OAuth flow
@param revocationURL the URL to revoke tokens, if supported by the flow
@param configuration the OAuth configuration for the client
--}}
self.clientId = {{{clientId}}}
self.clientSecret = {{{clientSecret}}}
self.configuration = {{{configuration}}}
self.revocationURL = {{{revocationURL}}}
